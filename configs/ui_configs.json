{
    "model" : {
        "default": "qwen3:4b",
        "help": "The LLM model that would be used for the task."
    },
    "temperature" : {
        "default": 0,
        "help": "The temperature for the LLM model."
    },
    "verbose": {
        "default": false,
        "help": "Enable verbose logging into agentic-fe.log" 
    },
    "username": {
        "default": "user",
        "help": "The name of the user."
    },
    "stm": {
        "default": true,
        "help": "Enable short-term memory."
    },
    "allow_clear_txt": {
        "default": false,
        "help": "Allow the agent to use the clear_txt tool to erase file contents."
    },
    "think": {
        "default": false,
        "help": "Enable extended thinking for the orchestrator model."
    }
}